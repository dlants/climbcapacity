{
  "commandAllowlist": [
    "^(cd [^;&|()<>]+ && )?npx tsc --noEmit$",
    "^(cd [^;&|()<>]+ && )?npx vitest run( [^;&|()<>]*)?$",
    "^yarn workspace [^;&|()<>]+ exec vitest run( [^;&|()<>]*)?$"
  ]
}
